// 1. Log the greeting message exactly as requested
console.log("Hello! I'm your coding fun fact guide!");

// 2. Create the three required variables as strings
const botName = "Saroj"; // Stores the bot's name
const botLocation = "the cloud"; // Stores where the bot is from
const favoriteLanguage = "JavaScript"; // Stores the favorite coding language

// 3. Log the name and location using the variables
console.log(`My name is ${botName} and I live on ${botLocation}.`);

// 4. Log the favorite language using the variable
console.log(`My favorite programming language is ${favoriteLanguage}.`);

// 5. Use 'let' to create the codingFact variable with the first fact
let codingFact = `${favoriteLanguage} was created in only 10 days!`;

// 6. Log the first codingFact to the console
console.log(codingFact);

// 7. Reassign codingFact to a second fact using the favoriteLanguage variable
codingFact = `Initially, ${favoriteLanguage} was going to be called Mocha.`;

// 8. Log the updated codingFact (second time)
console.log(codingFact);

// 9. Reassign codingFact to a third fact using the favoriteLanguage variable
codingFact = `Almost every modern website uses ${favoriteLanguage} for interactivity.`;

// 10. Log the updated codingFact (third time)
console.log(codingFact);

// 11. Log the farewell statement using botName and botLocation variables
console.log(`It was fun sharing these facts with you. Goodbye! - ${botName} from ${botLocation}.`);