// Line 1: Declare the 'adjective' variable using let and assign a string value.
let adjective = "tiny";

// Line 2: Declare the 'noun' variable using let and assign a string value.
let noun = "dragon";

// Line 3: Declare the 'verb' variable using let and assign a string value.
let verb = "excited";

// Line 4: Declare the 'place' variable using let and assign a string value.
let place = "castle";

// Line 5: Declare the 'adjective2' variable using let and assign a string value.
let adjective2 = "giant";

// Line 6: Declare the 'noun2' variable using let and assign a string value.
let noun2 = "tacos";

// Line 7: Declare the 'firstStory' variable using let to build the first narrative string.
let firstStory = `Once upon a time, there was a(n) ${adjective} ${noun} who loved to eat ${noun2}. The ${noun} lived in a ${place} and had ${adjective2} nostrils that blew fire when it was ${verb}.`;

// Line 8: Log the first story to the console using the exact required prefix text wrapper.
console.log(`First story: ${firstStory}`);

// Line 9: Reassign the 'adjective' variable with a brand new string value for the second story.
adjective = "grumpy";

// Line 10: Reassign the 'noun' variable with a brand new string value for the second story.
noun = "wizard";

// Line 11: Reassign the 'verb' variable with a brand new string value for the second story.
verb = "startled";

// Line 12: Reassign the 'place' variable with a brand new string value for the second story.
place = "cave";

// Line 13: Reassign the 'adjective2' variable with a brand new string value for the second story.
adjective2 = "smoking";

// Line 14: Reassign the 'noun2' variable with a brand new string value for the second story.
noun2 = "waffles";

// Line 15: Declare the 'secondStory' variable to assemble the second iteration of the template.
let secondStory = `Once upon a time, there was a(n) ${adjective} ${noun} who loved to eat ${noun2}. The ${noun} lived in a ${place} and had ${adjective2} nostrils that blew fire when it was ${verb}.`;

// Line 16: Log the second story to the console using the exact required prefix text wrapper.
console.log(`Second story: ${secondStory}`);